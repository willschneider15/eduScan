import { PublicKey } from "@solana/web3.js";
export declare const PROGRAM_ID: PublicKey;
export declare const TOKEN_METADATA_PROGRAM_ID: PublicKey;
/**
 * Derive the PDA for an Access program account.
 * @export
 * @param {PublicKey} wallet
 * @param {PublicKey} xnft
 * @returns {[PublicKey, number]}
 */
export declare function deriveAccessAddress(wallet: PublicKey, xnft: PublicKey): [PublicKey, number];
/**
 * Derive the PDA for an Install program account.
 * @export
 * @param {PublicKey} authority
 * @param {PublicKey} xnft
 * @returns {[PublicKey, number]}
 */
export declare function deriveInstallAddress(authority: PublicKey, xnft: PublicKey): [PublicKey, number];
/**
 * Derive the PDA of the master mint account.
 * @export
 * @param {string} name
 * @param {PublicKey} publisher
 * @returns {[PublicKey, number]}
 */
export declare function deriveMasterMintAddress(name: string, publisher: PublicKey): [PublicKey, number];
/**
 * Derive the PDA of a Review program account.
 * @export
 * @param {PublicKey} xnft
 * @param {PublicKey} author
 * @returns {[PublicKey, number]}
 */
export declare function deriveReviewAddress(xnft: PublicKey, author: PublicKey): [PublicKey, number];
/**
 * Derive the PDA of the associated xNFT program account.
 * @export
 * @param {PublicKey} masterMint
 * @returns {[PublicKey, number]}
 */
export declare function deriveXnftAddress(masterMint: PublicKey): [PublicKey, number];
//# sourceMappingURL=addresses.d.ts.map