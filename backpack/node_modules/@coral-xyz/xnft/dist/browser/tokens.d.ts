import type { ProgramAccount } from "@coral-xyz/anchor";
import { type RawAccount as TokenAccount } from "@solana/spl-token";
import { Connection, PublicKey } from "@solana/web3.js";
/**
 * Get the active NFT associated token account for the argued master mint public key
 * that has the token current in the balance.
 * @export
 * @param {Connection} connection
 * @param {PublicKey} mint
 * @returns {Promise<ProgramAccount<TokenAccount>>}
 */
export declare function getNftTokenAccountForMint(connection: Connection, mint: PublicKey): Promise<ProgramAccount<TokenAccount>>;
//# sourceMappingURL=tokens.d.ts.map