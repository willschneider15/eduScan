import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, useContext } from "react";
import { darkTheme } from "@coral-xyz/themes";
const _AnchorContext = createContext(null);
export const NAV_STACK = [];
export function AnchorProvider(props) {
    return (_jsx(_AnchorContext.Provider, { value: {
            theme: darkTheme,
        }, children: props.children }));
}
function useAnchorContext() {
    const ctx = useContext(_AnchorContext);
    if (ctx === null) {
        throw new Error("Context not available");
    }
    return ctx;
}
export function useTheme() {
    const { theme } = useAnchorContext();
    return theme;
}
//# sourceMappingURL=Context.js.map